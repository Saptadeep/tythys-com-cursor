export function HudPlaceholder() {
  return (
    <section className="card" style={{ padding: 14 }}>
      <h3 className="section-title">Hud Placeholder</h3>
      <p className="note" style={{ marginBottom: 0 }}>
        Reserved slot for system HUD widgets (health pulse, alerts, heartbeat graph, action shortcuts).
      </p>
    </section>
  )
}
