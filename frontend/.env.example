BACKEND_MODE=mock
BACKEND_BASE_URL=http://localhost:8080/v1

# Used by Next.js server components when calling local API routes during SSR
INTERNAL_APP_ORIGIN=http://127.0.0.1:3000

# Required for /api/ingest/latest proxy when BACKEND_MODE=real
INGEST_API_KEY=dev-ingest-key
